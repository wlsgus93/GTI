# Vercel 운영 환경변수 — Vercel dashboard 의 Environment Variables 에 등록 (.env.production 자동 X — Vercel UI 사용)
# 자체 도메인 + Cloudflare Tunnel 사용 시:
VITE_API_BASE_URL=https://api.gti.your-domain.com

# 또는 Cloudflare 부도메인 (도메인 없을 때):
# VITE_API_BASE_URL=https://gti-api.<your-tunnel>.cfargotunnel.com
