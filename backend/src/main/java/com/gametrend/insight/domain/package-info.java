/**
 * Domain layer (헥사고날 핵심).
 *
 * <p>Pure POJO 도메인 객체, 값 객체, 도메인 서비스. Spring 의존성 절대 없음.
 * JPA 어노테이션도 여기 두지 않음 (infrastructure/persistence/에 별도 엔티티).
 *
 * <p>하위 패키지: game, snapshot, analysis, watchlist, verification, campaign, user, common
 */
package com.gametrend.insight.domain;
